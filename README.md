TAU-SEN104 HTML Assignment

This repository contains my HTML assignment for TAU-SEN104.  
The main file is `index.html`, which demonstrates basic HTML structure including headings, paragraphs, and a linked JavaScript file and CSS file.

## Files

- `index.html` — Main HTML file  
- `script.js` —  JavaScript file linked to `index.html`  
- `styles.css` —  CSS file for styling
